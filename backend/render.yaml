services:
  - type: web
    name: skillcert-tracker-backend
    runtime: java
    plan: free
    rootDir: .
    buildCommand: mvn clean package
    startCommand: java -jar target/skillcert-tracker-backend-1.0.0.jar
